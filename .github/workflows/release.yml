name: Release Pipeline
on:
  push:
    branches: 
      - 'main'
    paths-ignore:
      - '.github/**'

  workflow_dispatch:
  
permissions:
  packages: write
  contents: write

jobs:
    preRelease:
      if: ${{ github.event_name == 'push' }}
      uses: baaskit-servicios-financieros-core/workflows/.github/workflows/preRelease.yml@main
    
    Release:
      uses: baaskit-servicios-financieros-core/workflows/.github/workflows/release.yml@main
